<UserControl x:Class="MessengerDesktop.Views.UserChatTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MessengerDesktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <UserControl.Resources>
        <Style TargetType="Grid" x:Key="HoverStyle">
            <Setter Property="Background" Value="{StaticResource MainBackgroundColor}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondBackgroundColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="MainGrid" Style="{StaticResource HoverStyle}" HorizontalAlignment="Left">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="18*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <TextBlock HorizontalAlignment="Center" 
                       TextWrapping="Wrap"  
                       Text="{Binding User.Name}" 
                       Foreground="{StaticResource MainTextColor}"
                       FontSize="16"
                       VerticalAlignment="Center" 
                       Width="216"
                       Margin="0,10,0,10"/>
            <TextBlock HorizontalAlignment="Center" 
                       TextWrapping="Wrap" 
                       Text="{Binding LastMessage.Message, UpdateSourceTrigger=PropertyChanged}" 
                       VerticalAlignment="Center" 
                       Width="216"
                       FontSize="14"
                       Foreground="{StaticResource SecondTextColor}"
                       Grid.Row="1"
                       Margin="0,0,0,10"/>
        </Grid>
        <Ellipse HorizontalAlignment="Center" Stroke="White" Fill="white" VerticalAlignment="Center" Width="50" Height="50" Margin="5,0,5,0"/>
    </Grid>
</UserControl>
