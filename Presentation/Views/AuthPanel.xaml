<UserControl x:Class="MessengerDesktop.Presentation.Views.AuthPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MessengerDesktop.Presentation.Views"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource MainBackgroundColor}">
        <TextBlock FontSize="24"
            TextAlignment="Left" 
            Foreground="{StaticResource MainTextColor}"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            TextWrapping="Wrap"
            Text="Введите ключ доступа" 
            Height="40"
            Width="700"
            Visibility="{Binding ElementName=EnterBox, Path=Text.IsEmpty, Converter={StaticResource BoolToVis}}" 
            IsHitTestVisible="False"
            Padding="10,2,0,0"/>

        <TextBox Name="EnterBox"
            FontSize="24"
            TextAlignment="Left" 
            Foreground="{StaticResource MainTextColor}"
            Background="{StaticResource EmptyObjectColor}"
            BorderThickness="2"
            BorderBrush="{StaticResource SecondBackgroundColor}"
            VerticalAlignment="Center"
            TextWrapping="Wrap"
            Text="{Binding PersonalKey, UpdateSourceTrigger=PropertyChanged}" 
            Height="40"
            Width="700"
            AcceptsReturn="False">
            <i:Interaction.Triggers>
                <i:KeyTrigger Key="Return">
                    <i:InvokeCommandAction Command="{Binding AuthCommand}"/>
                </i:KeyTrigger>
            </i:Interaction.Triggers>
        </TextBox>
        <Button Style="{StaticResource RoundedButton}" 
                Command="{Binding AuthCommand}"
                Content="Войти" 
                FontSize="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="0,0,0,120"
                Height="50"
                Width="200"/>
    </Grid>
</UserControl>
